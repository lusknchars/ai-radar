# O Jornal — Plano de Implementação

> **Para trabalhadores agênticos:** SUB-SKILL OBRIGATÓRIA: use
> superpowers:subagent-driven-development (recomendado) ou
> superpowers:executing-plans para implementar tarefa a tarefa. Os passos usam
> caixas (`- [ ]`) para rastreio.

**Objetivo:** uma página que estabelece o acervo do ai-radar — famílias, fronteira
entre construído e olhado, avanço alegado ao longo do tempo — gerada pelo próprio
pipeline e publicada no GitHub Pages a cada execução diária.

**Arquitetura:** o desenho é gerado em Python e sai como string. `render_site`
recebe um `SiteData` pronto e devolve HTML; os gráficos são SVG produzido por
funções puras. Nenhuma biblioteca de gráfico, nenhum framework, nenhum passo de
build. A interatividade é troca de `hidden` entre SVGs já renderizados.

**Stack:** Python 3.12, `pytest`. **Zero dependência nova**, no servidor e no
cliente.

**Spec:** `docs/2026-08-29-jornal-spec.md`

**PRÉ-REQUISITO BLOQUEANTE:** o plano `docs/plans/2026-08-29-segundo-escopo.md`
precisa estar concluído **e o gate da Tarefa 11 passo 5 precisa ter passado**.
Sem taxonomia fechada esta página é mil e oitenta e oito cards, que é o feed com
CSS. Se `pratica` colapsou acima de 45%, **não comece**: a tela herdaria o
defeito do eixo.

---

## Restrições globais

**Git exige aprovação por ação.** Nenhum `git add`, `git commit` ou `git push`
sem aprovação explícita do Lucas para aquela ação específica. Os passos de commit
são propostas.

**Sem trailer de co-autoria.** Mensagens em português, imperativo, sem emoji.

**A camada de render continua pura.** `render_site` e as funções de SVG não
importam `sqlite3`, `httpx` nem `anthropic`. Existe teste que trava isso, e ele
vale para os arquivos novos.

**O jornal nunca recebe o `Store`.** Ele recebe `SiteData`. A coleta mora no
store; o desenho não sabe de onde o dado veio.

**O rótulo "alegado pelos autores, não verificado" acompanha todo `ganho_fator`
que aparecer na página** — eixo, legenda, tabela, título. Sem exceção por falta
de espaço: se não couber o rótulo, corta-se o gráfico, não o rótulo.

**`citations = None` renderiza "—", nunca `0`.**

**Cor significa família, e só.** Em nenhum gráfico a cor codifica outra coisa.

**Sem dependência externa é literal:** fonte do sistema, nada de CDN, nada de
Google Fonts, nenhuma requisição saindo da página.

**A página funciona com JS desligado.** O JS só troca qual SVG está visível; com
ele desligado, o primeiro fica.

**Ambiente:** `/Users/luskoliveira/.pyenv/versions/3.12.3/bin/python`, testes com
`PYTHONPATH=src <python> -m pytest`.

---

## Estrutura de arquivos

| Arquivo | Responsabilidade | Tarefas |
|---|---|---|
| `src/radar/site_data.py` | **novo** — `SiteData`, `Ponto`, `SerieFamilia` | 1 |
| `src/radar/store.py` | leitura que monta o `SiteData` | 1 |
| `src/radar/svg.py` | **novo** — projeção, eixos, scatter, múltiplos | 2, 3 |
| `src/radar/site.py` | **novo** — `render_site`, seções, CSS | 4-8 |
| `src/radar/cli.py` | escreve `site/index.html` | 9 |
| `.github/workflows/radar.yml` | publica no Pages | 9 |

---

## Tarefa 1: `SiteData` e a leitura que o monta

**Arquivos:** Criar `src/radar/site_data.py`. Modificar `src/radar/store.py`.
Testes: `tests/test_site_data.py`, `tests/test_store.py`.

**Interfaces:**
- Produz: `SiteData`, `Ponto`, `Store.site_data() -> SiteData`
- Consumido por: todas as tarefas seguintes.

**Por que um tipo próprio e não `list[dict]`:** o desenho precisa saber os nomes
dos campos, e `dict` os esconde. Um erro de digitação em `p["familia"]` só
aparece em produção; em `p.familia` aparece no import.

- [ ] **Passo 1: escrever os testes que falham**

```python
# tests/test_site_data.py
from radar.site_data import Ponto, SiteData


def test_o_ponto_carrega_o_que_a_pagina_desenha():
    p = Ponto(arxiv_id="2608.1", titulo="T", familia="cache_kv",
              pratica="adotar", independent_impls=3, stars_total=10,
              citations=None, idade_dias=12, total_impls=4,
              ganho_eixo="velocidade", ganho_fator=2.3,
              ganho_texto="2.3x", resumo="r", publicado="2026-08-01",
              score=1.2, scope="inferencia")
    assert p.ganho_fator == 2.3
    assert p.citations is None


def test_a_cobertura_de_ganho_e_calculada_e_nao_informada():
    # O jornal decide sozinho se desenha a secao de avanco. Receber a
    # cobertura pronta permitiria que quem monta o SiteData mentisse.
    pontos = [_ponto(ganho_fator=2.0), _ponto(ganho_fator=None),
              _ponto(ganho_fator=None), _ponto(ganho_fator=None)]
    assert SiteData(pontos=pontos, dia="2026-08-29", cortes={},
                    rechecked_total=0).cobertura_de_ganho == 0.25


def test_acervo_vazio_tem_cobertura_zero_e_nao_explode():
    assert SiteData(pontos=[], dia="2026-08-29", cortes={},
                    rechecked_total=0).cobertura_de_ganho == 0.0


def test_familias_presentes_sai_ordenada_e_sem_repeticao():
    d = SiteData(pontos=[_ponto(familia="outro"), _ponto(familia="cache_kv"),
                         _ponto(familia="cache_kv")],
                 dia="2026-08-29", cortes={}, rechecked_total=0)
    assert d.familias_presentes == ["cache_kv", "outro"]
```

- [ ] **Passo 2: rodar e confirmar que falha**

Run: `PYTHONPATH=src <python> -m pytest tests/test_site_data.py -v`
Esperado: FAIL com `ModuleNotFoundError: No module named 'radar.site_data'`

- [ ] **Passo 3: implementar**

```python
# src/radar/site_data.py
"""O dado que a pagina desenha. Sem IO, sem dependencia externa."""
from __future__ import annotations

from dataclasses import dataclass, field


@dataclass(frozen=True)
class Ponto:
    arxiv_id: str
    titulo: str
    familia: str
    pratica: str
    independent_impls: int
    total_impls: int
    stars_total: int
    citations: int | None       # None = desconhecido; renderiza "—"
    idade_dias: int
    ganho_eixo: str
    ganho_fator: float | None
    ganho_texto: str
    resumo: str
    publicado: str              # ISO date
    score: float
    scope: str


@dataclass(frozen=True)
class SiteData:
    pontos: list[Ponto]
    dia: str
    cortes: dict[str, int]
    rechecked_total: int
    repos_do_destaque: list[dict] = field(default_factory=list)

    @property
    def cobertura_de_ganho(self) -> float:
        """Fracao dos papers com fator de ganho extraido.

        Calculada aqui e nao recebida pronta: e ela que decide se a secao de
        avanco existe, e quem monta o SiteData nao pode ter voz nisso.
        """
        if not self.pontos:
            return 0.0
        com = sum(1 for p in self.pontos if p.ganho_fator is not None)
        return com / len(self.pontos)

    @property
    def familias_presentes(self) -> list[str]:
        return sorted({p.familia for p in self.pontos})

    @property
    def destaque(self) -> Ponto | None:
        return max(self.pontos, key=lambda p: p.score, default=None)
```

E em `store.py`, `site_data(hoje)` que junta `papers`, o julgamento mais recente
de cada um, e o sinal mais recente de cada um. **O `idade_dias` é calculado a
partir de `published` e do dia passado por argumento** — nunca de
`date.today()`, que tornaria o teste dependente do relógio.

- [ ] **Passo 4: rodar a suíte**

Run: `PYTHONPATH=src <python> -m pytest -q`
Esperado: 252 passed, 5 skipped

- [ ] **Passo 5: propor commit**

```bash
git add src/radar/site_data.py src/radar/store.py tests/
git commit -m "feat: SiteData e a leitura que monta o acervo para a pagina"
```

---

## Tarefa 2: projeção e scatter em SVG

**Arquivos:** Criar `src/radar/svg.py`. Teste: `tests/test_svg.py`.

**Interfaces:**
- Produz: `projetar(valor, maximo, tamanho, pad, inverter) -> float`,
  `render_scatter(pontos, x_metrica, cores) -> str`
- Consumido por: tarefas 5 e 6.

**O ponto desta tarefa é que o gráfico vira código testado.** Uma biblioteca no
cliente tornaria a posição de cada ponto inauditável; aqui um teste afirma que o
paper X está na coordenada Y, e a projeção é aritmética.

- [ ] **Passo 1: escrever os testes que falham**

```python
# tests/test_svg.py
import pytest
from radar.svg import ALTURA, LARGURA, PAD, projetar, render_scatter


def test_o_maximo_cai_na_borda_util_direita():
    assert projetar(10, 10, LARGURA, PAD) == LARGURA - PAD


def test_o_zero_cai_na_borda_util_esquerda():
    assert projetar(0, 10, LARGURA, PAD) == PAD


def test_o_meio_cai_no_meio():
    assert projetar(5, 10, LARGURA, PAD) == PAD + (LARGURA - 2 * PAD) / 2


def test_o_eixo_invertido_espelha():
    # Em SVG o y cresce para baixo; o valor alto tem que subir.
    assert projetar(10, 10, ALTURA, PAD, inverter=True) == PAD


def test_maximo_zero_nao_divide_por_zero():
    # Acervo em que todo mundo tem zero estrela e caso real no dia 1.
    assert projetar(0, 0, LARGURA, PAD) == PAD


def test_o_scatter_desenha_um_circulo_por_ponto(tres_pontos):
    svg = render_scatter(tres_pontos, "stars_total", CORES)
    assert svg.count("<circle") == 3


def test_a_cor_do_circulo_vem_da_familia(tres_pontos):
    svg = render_scatter(tres_pontos, "stars_total", {"cache_kv": "#abc"})
    assert 'fill="#abc"' in svg


def test_o_scatter_vazio_gera_svg_valido_e_nao_explode():
    svg = render_scatter([], "stars_total", {})
    assert svg.startswith("<svg") and svg.endswith("</svg>")
    assert "<circle" not in svg


def test_metrica_desconhecida_e_recusada(tres_pontos):
    with pytest.raises(ValueError, match="metrica"):
        render_scatter(tres_pontos, "popularidade", CORES)


def test_citacao_nao_e_metrica_de_eixo(tres_pontos):
    # Decisao travada da spec: o acervo e jovem demais para o eixo
    # discriminar -- a esmagadora maioria empilharia no zero.
    with pytest.raises(ValueError):
        render_scatter(tres_pontos, "citations", CORES)
```

- [ ] **Passo 2: rodar e confirmar que falha**

Run: `PYTHONPATH=src <python> -m pytest tests/test_svg.py -v`
Esperado: FAIL com `ModuleNotFoundError: No module named 'radar.svg'`

- [ ] **Passo 3: implementar**

```python
# src/radar/svg.py
"""Desenho em SVG. Puro: sem IO, sem dependencia externa.

O grafico e gerado no servidor de proposito. Biblioteca de grafico no cliente
tiraria a posicao de cada ponto da fronteira testada do projeto -- e essa
fronteira e o que faz este repositorio confiavel.
"""
from __future__ import annotations

from html import escape

from .site_data import Ponto

LARGURA, ALTURA, PAD = 860, 480, 52

# Citacao esta fora de proposito: ver spec do jornal, secao 3.3.
METRICAS_X = {
    "stars_total": "estrelas no GitHub",
    "idade_dias": "dias desde a publicação",
    "total_impls": "implementações totais",
}


def projetar(valor: float, maximo: float, tamanho: int, pad: int,
             inverter: bool = False) -> float:
    """Projeta um valor no eixo. `maximo <= 0` colapsa na origem em vez de
    dividir por zero -- acervo em que ninguem tem estrela e caso real."""
    frac = 0.0 if maximo <= 0 else valor / maximo
    util = tamanho - 2 * pad
    return pad + util * (1 - frac) if inverter else pad + util * frac


def render_scatter(pontos: list[Ponto], x_metrica: str,
                   cores: dict[str, str]) -> str:
    if x_metrica not in METRICAS_X:
        raise ValueError(
            f"metrica {x_metrica!r} nao e eixo valido; "
            f"use um de {sorted(METRICAS_X)}"
        )
    partes = [f'<svg viewBox="0 0 {LARGURA} {ALTURA}" '
              f'role="img" aria-label="fronteira do acervo">']
    if pontos:
        max_x = max(getattr(p, x_metrica) for p in pontos)
        max_y = max(p.independent_impls for p in pontos)
        for p in pontos:
            cx = projetar(getattr(p, x_metrica), max_x, LARGURA, PAD)
            cy = projetar(p.independent_impls, max_y, ALTURA, PAD, inverter=True)
            partes.append(
                f'<circle cx="{cx:.1f}" cy="{cy:.1f}" r="4" '
                f'fill="{cores.get(p.familia, "currentColor")}" '
                f'opacity="0.8"><title>{escape(p.titulo)}</title></circle>'
            )
    partes.append("</svg>")
    return "".join(partes)
```

- [ ] **Passo 4: mutação — provar que a inversão está travada**

Troque `inverter=True` por `inverter=False` na chamada de `cy`. Rode
`pytest tests/test_svg.py`. Se **nenhum** teste falhar, falta o teste que
compara a coordenada de um ponto alto com a de um baixo — acrescente-o antes de
seguir. Restaure.

- [ ] **Passo 5: rodar a suíte**

Run: `PYTHONPATH=src <python> -m pytest -q`
Esperado: 262 passed, 5 skipped

- [ ] **Passo 6: propor commit**

```bash
git add src/radar/svg.py tests/test_svg.py
git commit -m "feat: scatter em SVG gerado no servidor"
```

---

## Tarefa 3: pequenos múltiplos por família

**Arquivos:** Modificar `src/radar/svg.py`. Teste: `tests/test_svg.py`.

**Interfaces:** Produz `render_pequenos_multiplos(series, familias, cores) -> str`,
onde `series` é `dict[str, dict[str, int]]` — família → mês ISO → contagem.

**Pequenos múltiplos e não área empilhada:** a pergunta é "esta família está
crescendo?", não "que fatia do total ela é". Empilhar faz cada série depender
das vizinhas e destrói a leitura individual.

**Escala compartilhada entre os painéis**, senão a comparação entre famílias
mente — uma família com 3 papers pareceria idêntica a uma com 300.

- [ ] **Passo 1: escrever os testes que falham**

```python
def test_um_painel_por_familia_presente():
    s = {"cache_kv": {"2026-07": 3}, "outro": {"2026-07": 1}}
    svg = render_pequenos_multiplos(s, ["cache_kv", "outro"], CORES)
    assert svg.count("<g class=\"painel\"") == 2


def test_a_escala_e_compartilhada_entre_os_paineis():
    # Escala por painel faria 3 papers e 300 desenharem a mesma altura.
    s = {"a": {"2026-07": 1}, "b": {"2026-07": 100}}
    svg = render_pequenos_multiplos(s, ["a", "b"], {})
    alturas = [float(x) for x in _alturas_das_barras(svg)]
    assert alturas[0] < alturas[1] / 10


def test_familia_sem_dado_ainda_ganha_painel_vazio():
    # Ausencia e informacao: um painel vazio diz "nada saiu nesta familia".
    svg = render_pequenos_multiplos({"a": {"2026-07": 2}}, ["a", "b"], {})
    assert svg.count("<g class=\"painel\"") == 2
```

- [ ] **Passo 2: rodar e confirmar que falha**

Run: `PYTHONPATH=src <python> -m pytest tests/test_svg.py -v -k multiplos`
Esperado: FAIL com `ImportError`

- [ ] **Passo 3: implementar**

Grade de painéis com `viewBox` próprio por painel, barra por mês, `max` global
calculado sobre todas as séries antes de desenhar qualquer uma.

- [ ] **Passo 4: rodar a suíte**

Run: `PYTHONPATH=src <python> -m pytest -q`
Esperado: 265 passed, 5 skipped

- [ ] **Passo 5: propor commit**

```bash
git add src/radar/svg.py tests/test_svg.py
git commit -m "feat: pequenos multiplos por familia com escala compartilhada"
```

---
## Tarefa 4: o esqueleto de `render_site`

**Arquivos:** Criar `src/radar/site.py`. Teste: `tests/test_site.py`.

**Interfaces:**
- Produz: `render_site(dados: SiteData) -> str`, `CORES_FAMILIA`
- Consumido por: tarefas 5 a 9.

**Esta tarefa entrega a página inteira com as seções vazias.** As tarefas 5 a 8
preenchem uma cada. É o que permite revisar identidade visual antes de haver
conteúdo para discutir.

- [ ] **Passo 1: escrever os testes que falham**

```python
# tests/test_site.py
import re
from radar.site import CORES_FAMILIA, render_site
from radar.models import FAMILIAS


def test_a_pagina_e_html_completo(dados):
    html = render_site(dados)
    assert html.lstrip().startswith("<!doctype html>")
    assert "</html>" in html


def test_toda_familia_tem_cor_propria():
    assert set(CORES_FAMILIA) == FAMILIAS
    assert len(set(CORES_FAMILIA.values())) == len(FAMILIAS)


def test_a_pagina_nao_faz_requisicao_externa(dados):
    # "Sem dependencia externa" e literal: nada de CDN, nada de fonte remota.
    html = render_site(dados)
    for proibido in ("https://", "http://", "//cdn", "@import"):
        assert proibido not in html.replace("https://arxiv.org", "")


def test_a_pagina_define_tema_claro_e_escuro(dados):
    html = render_site(dados)
    assert "prefers-color-scheme: dark" in html


def test_acervo_vazio_gera_pagina_valida(dados_vazio):
    # Nao excecao, nao HTML quebrado: a pagina diz que nao ha dado.
    html = render_site(dados_vazio)
    assert "</html>" in html
    assert "nenhum paper" in html.lower()


def test_o_enquadramento_esta_presente_e_e_fixo(dados):
    # Contrato com o leitor: o que o radar mede e o que ele deliberadamente
    # nao mede. Nao e gerado, e versionado.
    html = render_site(dados)
    assert "implementações independentes" in html
    assert "não mede" in html


def test_o_site_nao_importa_io():
    import radar.site as m
    fonte = open(m.__file__).read()
    for proibido in ("import sqlite3", "import httpx", "import anthropic"):
        assert proibido not in fonte
```

- [ ] **Passo 2: rodar e confirmar que falha**

Run: `PYTHONPATH=src <python> -m pytest tests/test_site.py -v`
Esperado: FAIL com `ModuleNotFoundError: No module named 'radar.site'`

- [ ] **Passo 3: implementar**

`CORES_FAMILIA` com 19 cores distintas, e o esqueleto:

```python
def render_site(dados: SiteData) -> str:
    if not dados.pontos:
        corpo = '<p class="vazio">Nenhum paper no acervo ainda.</p>'
    else:
        corpo = "".join((
            _secao_fronteira(dados),
            _secao_avanco(dados),
            _secao_familias(dados),
            _secao_tabela(dados),
            _secao_destaque(dados),
            _secao_cortes(dados),
        ))
    return (f"<!doctype html><html lang=\"pt-BR\"><head>"
            f"<meta charset=\"utf-8\">"
            f"<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">"
            f"<title>ai-radar — {dados.dia}</title>"
            f"<style>{_CSS}</style></head><body>"
            f"{_cabecalho(dados)}{_enquadramento()}{corpo}"
            f"<script>{_JS}</script></body></html>")
```

O CSS usa tokens em `:root`, redefinidos em
`@media (prefers-color-scheme: dark)`, e **pilha de fonte do sistema**:

```css
font-family: system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
```

- [ ] **Passo 4: rodar a suíte**

Run: `PYTHONPATH=src <python> -m pytest -q`
Esperado: 272 passed, 5 skipped

- [ ] **Passo 5: olhar a página**

```bash
PYTHONPATH=src <python> -c "
from radar.store import Store
from radar.site import render_site
import datetime
d = Store('data/radar.db').site_data(datetime.date(2026,8,29))
open('/tmp/jornal.html','w').write(render_site(d))
" && open /tmp/jornal.html
```

**Pare aqui e mostre ao Lucas.** Identidade visual é decisão dele, e é mais
barato ajustar com as seções vazias do que depois de preenchidas.

- [ ] **Passo 6: propor commit**

```bash
git add src/radar/site.py tests/test_site.py
git commit -m "feat: esqueleto do jornal com tema, tokens e enquadramento"
```

---

## Tarefa 5: a seção da fronteira

**Arquivos:** Modificar `src/radar/site.py`. Teste: `tests/test_site.py`.

**Interfaces:** Consome `render_scatter` (Tarefa 2) e `METRICAS_X`.

**A interatividade inteira:** três SVGs renderizados, um visível, e o JS troca
qual. Com JS desligado o primeiro fica — é por isso que ele não é `display:none`
por CSS externo, e sim atributo `hidden` no HTML.

- [ ] **Passo 1: escrever os testes que falham**

```python
def test_a_fronteira_traz_um_svg_por_metrica(dados):
    html = render_site(dados)
    assert html.count('class="scatter"') == 3


def test_so_o_primeiro_scatter_comeca_visivel(dados):
    html = render_site(dados)
    assert html.count('class="scatter" hidden') == 2


def test_o_portao_de_estouro_esta_rotulado(dados):
    # Quem olha precisa entender por que um paper muito citado nao aparece,
    # sem ler documentacao.
    html = render_site(dados)
    assert "1000" in html and "estourou" in html.lower()


def test_ha_um_botao_por_metrica(dados):
    html = render_site(dados)
    for rotulo in ("estrelas no GitHub", "dias desde a publicação",
                   "implementações totais"):
        assert rotulo in html


def test_a_legenda_lista_so_as_familias_presentes(dados):
    html = render_site(dados)
    assert "cache_kv" in html
    assert "destilacao" not in html      # ausente do acervo de teste
```

- [ ] **Passo 2 a 4:** rodar (FAIL), implementar `_secao_fronteira`, rodar a
      suíte. Esperado ao fim: 277 passed, 5 skipped.

- [ ] **Passo 5: propor commit**

```bash
git add src/radar/site.py tests/test_site.py
git commit -m "feat: secao da fronteira com eixo alternavel"
```

---

## Tarefa 6: a seção de avanço, e a condição que a apaga

**Arquivos:** Modificar `src/radar/site.py` e `src/radar/svg.py`.
Teste: `tests/test_site.py`.

**Interfaces:** Produz `render_avanco(pontos, cores) -> str` em `svg.py`.

**A regra mais importante da página está aqui.** Todo `ganho_fator` visível
carrega o rótulo, e a seção some quando a cobertura fica abaixo de 35%.

- [ ] **Passo 1: escrever os testes que falham**

```python
def test_todo_ganho_visivel_carrega_o_rotulo(dados):
    html = render_site(dados)
    assert "alegado pelos autores, não verificado" in html


def test_a_secao_some_com_cobertura_abaixo_de_35_por_cento(dados_ganho_ralo):
    # Grafico sobre dado ralo e pior que grafico ausente.
    assert dados_ganho_ralo.cobertura_de_ganho < 0.35
    html = render_site(dados_ganho_ralo)
    assert "avanço alegado" not in html.lower()


def test_sem_secao_de_avanco_nao_ha_rotulo_orfao(dados_ganho_ralo):
    html = render_site(dados_ganho_ralo)
    assert "alegado pelos autores" not in html


def test_a_escala_log_recusa_fator_nao_positivo():
    # Um zero aqui viraria coordenada NaN no SVG, e o grafico sairia
    # silenciosamente errado em vez de quebrar.
    with pytest.raises(ValueError):
        render_avanco([_ponto(ganho_fator=0.0)], CORES)


def test_a_mediana_trimestral_exige_cinco_papers():
    # Abaixo disso e ruido com aparencia de tendencia.
    quatro = [_ponto(ganho_fator=2.0, publicado="2026-07-01") for _ in range(4)]
    assert 'class="mediana"' not in render_avanco(quatro, CORES)
    cinco = quatro + [_ponto(ganho_fator=2.0, publicado="2026-07-01")]
    assert 'class="mediana"' in render_avanco(cinco, CORES)
```

- [ ] **Passo 2 a 4:** rodar (FAIL), implementar, rodar a suíte.
      Esperado: 283 passed, 5 skipped.

- [ ] **Passo 5: mutação — o rótulo é o ponto**

Apague o rótulo da f-string. Rode `pytest tests/test_site.py -k rotulo`. Deve
**falhar**. Restaure. Este teste é o que impede a regra de se perder numa
refatoração futura, e é a razão de ele existir separado.

- [ ] **Passo 6: propor commit**

```bash
git add src/radar/site.py src/radar/svg.py tests/
git commit -m "feat: secao de avanco alegado, condicional a cobertura do dado"
```

---

## Tarefa 7: famílias no tempo, e a tabela

**Arquivos:** Modificar `src/radar/site.py`. Teste: `tests/test_site.py`.

**Interfaces:** Consome `render_pequenos_multiplos` (Tarefa 3).

**O filtro primário é `pratica`**, não família: é ele que responde "o que eu
adoto". Sem estado, sem URL, sem framework — atributo `hidden` em linha.

- [ ] **Passo 1: escrever os testes que falham**

```python
def test_a_tabela_traz_uma_linha_por_paper(dados):
    assert render_site(dados).count('class="linha"') == len(dados.pontos)


def test_citacao_desconhecida_vira_travessao_e_nao_zero(dados_sem_citacao):
    html = render_site(dados_sem_citacao)
    assert "—" in html
    assert ">0<" not in html


def test_cada_linha_tem_link_de_arxiv_resolvivel(dados):
    html = render_site(dados)
    for p in dados.pontos:
        assert f"https://arxiv.org/abs/{p.arxiv_id}" in html


def test_a_linha_carrega_familia_e_pratica_como_atributo(dados):
    # E o que o filtro le; sem isso o JS teria que parsear texto.
    html = render_site(dados)
    assert 'data-familia="cache_kv"' in html
    assert 'data-pratica="adotar"' in html


def test_a_tabela_vem_ordenada_por_score(dados):
    html = render_site(dados)
    posicoes = [html.index(p.arxiv_id) for p in
                sorted(dados.pontos, key=lambda p: -p.score)]
    assert posicoes == sorted(posicoes)
```

- [ ] **Passo 2 a 4:** rodar (FAIL), implementar `_secao_familias` e
      `_secao_tabela`, rodar a suíte. Esperado: 288 passed, 5 skipped.

- [ ] **Passo 5: propor commit**

```bash
git add src/radar/site.py tests/test_site.py
git commit -m "feat: familias no tempo e tabela filtravel por pratica"
```

---

## Tarefa 8: ponta a ponta, e os cortes

**Arquivos:** Modificar `src/radar/site.py`. Teste: `tests/test_site.py`.

**Por que a seção de ponta a ponta existe:** sem ela, "3 implementações
independentes" é fé. Ela mostra os repositórios, o dono, as estrelas, e **qual
regra de autoria classificou cada um** — o número vira auditável.

- [ ] **Passo 1: escrever os testes que falham**

```python
def test_o_destaque_e_o_de_maior_score(dados):
    html = render_site(dados)
    melhor = max(dados.pontos, key=lambda p: p.score)
    assert melhor.resumo in html


def test_o_destaque_mostra_a_regra_de_autoria_de_cada_repo(dados_com_repos):
    # Sem isso o numero de implementacoes independentes e fe.
    html = render_site(dados_com_repos)
    assert "sobrenome do autor no dono" in html


def test_todos_os_cortes_do_dia_aparecem_com_contagem(dados):
    # Restricao global do projeto: todo corte contado chega ao leitor.
    html = render_site(dados)
    for motivo, n in dados.cortes.items():
        assert motivo in html
        assert str(n) in html


def test_dia_sem_corte_nenhum_ainda_mostra_a_secao(dados_sem_cortes):
    assert "Cortes" in render_site(dados_sem_cortes)


def test_acervo_sem_destaque_nao_explode(dados_vazio):
    assert "</html>" in render_site(dados_vazio)
```

- [ ] **Passo 2 a 4:** rodar (FAIL), implementar, rodar a suíte.
      Esperado: 293 passed, 5 skipped.

- [ ] **Passo 5: propor commit**

```bash
git add src/radar/site.py tests/test_site.py
git commit -m "feat: destaque auditavel e contabilidade de cortes"
```

---

## Tarefa 9: escrever o arquivo e publicar

**Arquivos:** Modificar `src/radar/cli.py` e `.github/workflows/radar.yml`.
Teste: `tests/test_cli.py`.

**GitHub Pages por Actions**, não por branch e pasta: o modo por pasta só aceita
a raiz ou `/docs`, e `/docs` é das specs.

**O ensaio a seco não escreve a página.** Ele já não escreve markdown nem banco;
a página segue a mesma regra.

- [ ] **Passo 1: escrever os testes que falham**

```python
def test_a_execucao_escreve_a_pagina(tmp_path):
    _executar(args, db_path, hoje)
    assert (tmp_path / "site" / "index.html").exists()


def test_o_ensaio_a_seco_nao_escreve_a_pagina(tmp_path):
    _executar(args_dry_run, db_path, hoje)
    assert not (tmp_path / "site").exists()
```

- [ ] **Passo 2: rodar e confirmar que falha**

Run: `PYTHONPATH=src <python> -m pytest tests/test_cli.py -v -k pagina`
Esperado: FAIL — nada escreve `site/`.

- [ ] **Passo 3: implementar na CLI**

```python
    if not args.dry_run:
        pagina = RAIZ / "site"
        pagina.mkdir(parents=True, exist_ok=True)
        (pagina / "index.html").write_text(
            render_site(store.site_data(hoje)), encoding="utf-8")
```

- [ ] **Passo 4: o workflow**

No `.github/workflows/radar.yml`, o job ganha permissão e passos de Pages:

```yaml
permissions:
  contents: write
  pages: write
  id-token: write
```

```yaml
      - name: Publicar no Pages
        if: ${{ !cancelled() }}
        uses: actions/upload-pages-artifact@v3
        with:
          path: site
```

mais o job `deploy` com `actions/deploy-pages@v4` e
`environment: github-pages`.

O passo de commit existente passa a incluir `site`:

```yaml
          git add -A -- data radar site
```

**`git add -A -- <dir>` e não `git add <dir>/<arquivo>`** — a forma com caminho
de arquivo falha quando o arquivo ainda não existe, e `mkdir -p` **não**
resolve: ele cria o diretório, não o arquivo. Isso já foi testado neste
repositório e a conclusão está registrada aqui para não se repetir.

- [ ] **Passo 5: rodar a suíte**

Run: `PYTHONPATH=src <python> -m pytest -q`
Esperado: 295 passed, 5 skipped

- [ ] **Passo 6: ligar o Pages e verificar ao vivo**

O Pages precisa estar em **Settings → Pages → Source: GitHub Actions** no
repositório, e isso é ação manual do Lucas. Depois de uma execução, confira que
a URL responde e que o tema escuro funciona.

- [ ] **Passo 7: propor commit**

```bash
git add src/radar/cli.py .github/workflows/radar.yml tests/test_cli.py
git commit -m "feat: publica o jornal no GitHub Pages a cada execucao"
```

---

## Auto-revisão do plano

**Cobertura da spec.** §1 (o que é, e o rótulo de alegação) → Tarefas 4 e 6.
§2 (tecnologia, sem dependência) → Tarefas 2, 3, 4, e o teste de requisição
externa na 4. §2 (onde mora) → Tarefa 9. §3.1 e §3.2 (cabeçalho e
enquadramento) → Tarefa 4. §3.3 (fronteira) → Tarefas 2 e 5. §3.4 (avanço) →
Tarefa 6. §3.5 (famílias no tempo) → Tarefas 3 e 7. §3.6 (tabela) → Tarefa 7.
§3.7 (ponta a ponta) → Tarefa 8. §3.8 (cortes) → Tarefa 8. §4 (identidade) →
Tarefa 4. §5 (fronteiras de código) → Tarefas 1, 2, 4, e o teste de pureza na
4. §6 (testes) → todos: teste 1 na Tarefa 4, 2 na 2, 3 na 6, 4 na 6, 5 na 6,
6 nas 4 e 8, 7 na 6, 8 na 7.

**Consistência de tipos.** `SiteData` e `Ponto` nascem na Tarefa 1 e são o único
insumo de tudo depois. `cobertura_de_ganho` é propriedade calculada, não campo —
quem monta o `SiteData` não pode influenciar se a seção de avanço existe.
`render_scatter` recusa `citations` como métrica na Tarefa 2, e a decisão está
travada por teste, não por convenção.

**A dependência que não pode ser ignorada.** Este plano inteiro pressupõe o gate
da Tarefa 11 do plano anterior. Se `pratica` colapsou acima de 45%, a Tarefa 7
constrói um filtro primário que não filtra nada, e a página fica bonita e inútil.

**Uma escolha que vale registrar.** A Tarefa 4 entrega a página inteira com as
seções vazias e **para para revisão visual**. É o único ponto do plano que
bloqueia esperando pessoa, e é deliberado: identidade visual é decisão do Lucas,
e ajustar tokens com as seções vazias custa uma fração de ajustá-los depois.

## Ordem de execução

1 → 9, com a Tarefa 4 abrindo um portão humano no passo 5.

As tarefas 5 a 8 são independentes entre si — cada uma preenche uma seção do
esqueleto e nenhuma lê o resultado da outra. Se houver pressa, elas podem ser
paralelizadas depois que a 4 estiver revisada; a 9 espera todas.
