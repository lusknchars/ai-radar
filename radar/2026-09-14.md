# Radar — 2026-09-14

## Inferência

### Radar

#### 1. Self-evolving procedural graph for agent guidance

Replaces unconstrained next-action generation over an accumulating trajectory with an evolving (procedure, relation, procedure) graph that a guidance model turns into step-level hints biasing, but not dictating, the agent's next action. Costs an extra guidance-model call per decision step plus LLM-refiner passes that contrast failed and successful trajectories to edit the graph against a held-out validation set, adding orchestration complexity and API spend. Can fail if validation sets are small or unrepresentative, letting the refiner overfit graph edits, or if localization of the active node is wrong on long trajectories, injecting misleading guidance; the abstract reports only qualitative 'consistent gains' with no quantified improvement, so the benefit is unverified for any specific workload.

- score: 1.5421
- 5 -> 9 impls independentes em 2 dias · 6 estrelas
- familia: memoria_e_contexto
- pratica: testar (The method is API-compatible and needs no training or cluster, but the abstract provides no quantified gains, so adoption should follow a workload-specific A/B evaluation against the reader's existing memory or prompting baseline.)
- arxiv.org/abs/2609.09153
  - danielsimonjr/MemoryJS — 4 estrelas — autor (mais_antigo_e_mais_estrelado)
  - InsomaniacElf/sg-tamil-tts-resources- — 1 estrelas — independente
  - acc0mplish/Effort-Router — 1 estrelas — independente
  - CaganAkaltun/procedural-graph-lab — 0 estrelas — independente
  - FammasMaz/pi-procedural-graphs — 0 estrelas — independente
  - biosync-tech/smriti — 0 estrelas — independente
  - hallieren/substack-data — 0 estrelas — independente
  - hemangnagar/retro — 0 estrelas — independente
  - jlove2177-jpg/trit-proc — 0 estrelas — independente
  - preprint-labs/procedural-graphs — 0 estrelas — independente

#### 2. Step-labeled injection-hijack trajectory benchmark (AgentDrift)

AgentDrift replaces whole-trace guard judgments and attack-success-only benchmarks with a step-labeled corpus (12,536 trajectories, 71,024 steps labeled benign, injection point, hijacked, or failed injection) for training and evaluating detectors of indirect prompt injection in tool-call trajectories. Adoption costs the integration of an offline evaluation harness against one's own agent traces plus detector development, since the paper provides data and a weak baseline rather than a deployable guard model. Key failure risks include domain mismatch with the five covered domains, synthetic-data artifacts the authors themselves document (template concentration, attack-goal-family collapse, world-identity leakage, an LLM judge fooled by hard negatives), and reduced validity against adaptive attackers once the corpus is public.

- score: 1.0327
- 4 -> 4 impls independentes em 2 dias · 2 estrelas
- familia: seguranca_e_guardrails
- pratica: testar (The corpus is a free, API-compatible resource for evaluating injection defenses on constrained infrastructure, but its documented synthetic-data limitations and lack of a trained detector mean it must be validated against the reader's own agent workloads before informing production guardrails.)
- arxiv.org/abs/2609.06972
  - Asif-0209/AgentDrift — 1 estrelas — independente
  - Ytang520/prompt_injection_research_daily_arxiv — 1 estrelas — independente
  - grrlkk/writing-agent-arxiv-daily — 0 estrelas — independente
  - shaijing/arxiv-paper — 0 estrelas — independente

#### 3. Context-asymmetric speculative decoding

AsymSpec replaces the standard lossy choice between input compression and full-context inference by letting a lightweight drafter read the full input while the large verifier generates against a compressed view, fusing their logits with a contrastive delta-fusion and a divergence-aware acceptance gate. The cost is running two models (drafter plus verifier) with dual context representations and a non-standard decoding loop, which increases memory footprint and implementation complexity relative to plain compression or off-the-shelf speculative decoding served through third-party APIs. It can fail when the divergence gate misjudges draft reliability, collapsing acceptance rates and erasing the speedup, and the reported gains are on isolated capabilities and two benchmarks, so end-to-end agentic workloads with heavy tool-call branching may see smaller benefits.

- score: 0.8737
- 2 -> 2 impls independentes em 2 dias · 1 estrelas
- familia: decodificacao_especulativa
- pratica: testar (The approach is implementable on a single 24 GB GPU with a small drafter plus verifier and directly targets the compression-versus-accuracy trade-off common in agentic pipelines, but its custom fusion and gating logic and its unvalidated behavior on the reader's specific workloads mean it warrants a controlled pilot rather than immediate production adoption.)
- ganho 1.7x em velocidade — alegado pelos autores, nao verificado
- arxiv.org/abs/2608.26004
  - InsomaniacElf/sg-tamil-tts-resources- — 1 estrelas — autor (mais_antigo_e_mais_estrelado)
  - USTC-StarTeam/AsymSpec — 0 estrelas — independente
  - halofx82/AsymSpec — 0 estrelas — independente

### Feed

Nada novo no escopo hoje.

### Re-consulta

30 papers re-consultados. 2 com movimento:

- 2609.09153 — 5 -> 9 impls independentes em 2 dias — score 1.5421
- 2609.00749 — 1 -> 2 impls independentes em 2 dias — score 0.3962

### Cortes

- exa_out_of_scope: 2
- ja_conhecido: 8
- reconsulta_abaixo_do_piso: 25
- reconsulta_fora_do_top3: 2
- termo_adiado: 10
- termo_falhou: 2


## Agentes

### Radar

Nenhum item passou o piso hoje.

### Feed

Nada novo no escopo hoje.

### Cortes

- ja_conhecido: 10
- termo_adiado: 15
- termo_falhou: 2

